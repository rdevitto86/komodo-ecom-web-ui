# Mock mode env vars — used by `bun run build:demo` (vite build --mode mock)
# Public vars (exposed to client) must be prefixed PUBLIC_
# Expand this file as mock API layers are added.

PUBLIC_API_URL=http://localhost:7001/mock
APP_NAME=komodo-ui
VERSION=0.0.0-mock
LOG_LEVEL=debug
